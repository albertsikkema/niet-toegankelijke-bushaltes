info:
  name: Service Descriptor
  type: http
  seq: 3

http:
  method: GET
  url: {{baseUrl}}/sherpa.json

docs: |-
  Sherpa service descriptor (GET, not POST). Returns API metadata:
  base URL, version, list of available function names, and environment config.
